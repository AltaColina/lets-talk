<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:messaging="clr-namespace:LetsTalk.Messaging;assembly=LetsTalk.Domain"
    xmlns:views="clr-namespace:LetsTalk.Views"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="LetsTalk.Views.ImageContentView"
    x:DataType="messaging:ContentMessage">
    <ContentView.Resources>
        <toolkit:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageSourceConverter" />
    </ContentView.Resources>
    <Frame
        HorizontalOptions="Center"
        Margin="2"
        Padding="10">
        <Grid
            RowDefinitions="16,256">
            <views:ContentMessageHeaderView
                Grid.Row="0"/>
            <Image
                Grid.Row="1"
                HorizontalOptions="StartAndExpand"
                Source="{Binding Content, Converter={StaticResource ByteArrayToImageSourceConverter}}"/>
        </Grid>
    </Frame>
</ContentView>
